# Completed Exec Plans

This folder is historical evidence for shipped or superseded work.

Some older files still contain their original internal status lines such as "active", "implementation-active", or "draft" because they were written while the work was underway. Folder location is authoritative after the 2026-05-17 cleanup: if a plan is in this directory, it is not part of the current active work queue.

Current work is tracked in `../active/roadmap.md` and `../active/completion-control.md`.
